import 'package:flutter/material.dart';

class LoginPage extends StatelessWidget {
  const LoginPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      // TODO-2: Change to login screen
      // add a StreamBuilder and stream FirebaseAuth.instance.authStateChanges()
      body: Center(
        child: Text("Empty"),
      ),
    );
  }
}
